# Generated by `eslint --suppress-all`, byte-for-byte. Reformatting it (prettier
# always adds a final newline, which .editorconfig deliberately withholds from
# JSON) makes the file differ from what the lint gate writes inside the runner,
# so the "baseline shrank" notice would fire on every PR and stop meaning
# anything. Regenerate it with eslint, never with a formatter.
eslint-suppressions.json
